<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 圆角 -->
    <corners
        android:radius="15dp"

/><!-- 设置圆角半径 -->
    <!--android:topLeftRadius="5dp"-->
    <!--android:topRightRadius="5dp"-->
    <!--android:bottomLeftRadius="5dp"-->
    <!--android:bottomRightRadius="5dp"-->

    <!-- 渐变 -->
    <!--<gradient-->
        <!--android:startColor="@color/lighter_gray"-->
        <!--android:endColor="@color/bg_color"-->
        <!--android:centerX="0.5"-->
        <!--android:centerY="0.5"-->
        <!--android:gradientRadius="70"-->
        <!--android:type="radial"-->
       <!--/>-->
    <!--android:centerColor="@android:color/holo_blue_light"-->

    <!-- 间隔 -->
    <padding
        android:left="1dp"
        android:top="1dp"
        android:right="1dp"
        android:bottom="1dp"/><!-- 各方向的间隔 -->

    <!-- 大小 -->
    <size
        android:width="60dp"
        android:height="30dp"/><!-- 宽度和高度 -->

    <!--&lt;!&ndash; 填充 &ndash;&gt;-->
    <solid
        android:color="@color/cp_divider"/><!-- 填充的颜色 -->

    <!-- 描边，边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/shenGray"
        android:dashWidth="1dp"
        android:dashGap="0dp"/>
</shape>